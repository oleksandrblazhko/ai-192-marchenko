## Варіант 18
Практика тестування безпеки (ST) використовує той факт, що, хоча автоматизоване тестування безпеки є швидким і добре масштабується до багатьох додатків, поглиблене тестування, засноване на хорошому знанні програми та її бізнес-логіки, часто можливе лише за допомогою повільнішого ручного експертного тестування безпеки. Тому в основі кожного потоку лежить певний підхід.

Перший напрямок зосереджується на створенні спільної базової лінії безпеки для автоматичного виявлення так званих "низько висячих плодів". Поступово налаштовуйте автоматизовані тести для кожного додатка і збільшуйте частоту їх виконання щоб виявити більше помилок і регресій раніше, якомога ближче до їх до їх виникнення. Чим більше помилок можуть виявити автоматизовані процеси, тим більше часу експерти мають на те, щоб використати свої знання та творчий підхід, щоб зосередитися на більш складних векторах атак і забезпечити поглиблене тестування додатків у другому потоці. Оскільки ручна перевірка повільна і важко масштабується, експерти визначають пріоритетність тестування компонентів на основі їх ризику, нещодавніх релевантних змін або майбутніх великих майбутніх великих випусків. Організації також можуть отримати доступ до зовнішньої експертизи, беручи участь наприклад, у програмах винагороди за виправлення помилок.

На відміну від тестування на основі вимог, яке фокусується на перевірці того, що додатки правильно реалізують свої вимоги, мета цієї практики - виявити слабкі місця в технічній і бізнес-логіці додатку і зробити їх видимими для керівництва і зацікавлених сторін бізнесу, незалежно від вимог.

### Потік A - Масштабована базова лінія
Масштабована базова лінія фокусується на використанні автоматизованого тестування для конкретних додатків інструментів, які інтегрують перевірку безпеки в процес збірки та розгортання. Мета цього потоку - надати перевагу ширині (широкому спектру додатків) над глибиною тестування.

### Потік B - Глибоке розуміння
Глибоке розуміння фокусується на виконанні ручного тестування безпеки компонентів з високим рівнем ризику, використовуючи складні вектори атак з метою зробити поглиблене тестування безпеки невід'ємною частиною процесу розробки. Мета цього потоку - надати перевагу глибині тестування (ретельності тестування) над шириною тестування (портфоліо додатків).

| Огляд діяльності      | Потік А                                                                             | Потік Б                                                                                    |
|-----------------------|-------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------|
| Рівень зрілості 1     | Виконуйте тестування безпеки (як вручну, так і за допомогою інструментів) для виявлення дефектів безпеки. Використовуйте автоматизовані інструменти тестування безпеки. | Виконуйте ручне тестування безпеки компонентів з високим рівнем ризику.                |
| Рівень зрілості 2     | Зробіть тестування безпеки під час розробки більш повним та ефективним завдяки автоматизації, доповнивши його регулярними ручними тестами на проникнення. Використовуйте тестування безпеки для конкретних додатків автоматизацію. | Проводьте тестування на проникнення вручну.                                                |
| Рівень зрілості 3     | Впроваджуйте тестування безпеки як частину процесів розробки та розгортання. Інтегруйте автоматизоване тестування безпеки в процес збірки та розгортання. | Інтегруйте тестування безпеки в процес розробки процес розробки.                          |

# Потік А - Масштабована базова лінія

## Верифікація

### Рівень зрілості 1

#### Перевага

- Виявлення поширених вразливостей, які легко знайти

#### Діяльність

Використовуйте автоматизовані інструменти статичного та динамічного тестування безпеки для програмного забезпечення, що дає змогу більш ефективне тестування безпеки та більш якісні результати. Поступово збільшуйте частоту тестів безпеки та розширюйте покриття коду.

Тестування безпеки додатків можна виконувати статично, шляхом перевірки вихідного коду програми без її запуску, або динамічно, шляхом простого спостерігання за поведінкою програми у відповідь на різні вхідні умови.

Перший підхід часто називають статичним тестуванням безпеки додатків (SAST), а другий - динамічним тестуванням безпеки додатків (DAST). Гібридний підхід, відомий як Інтерактивне тестування безпеки додатків (IAST), поєднує в собі сильні сторони обох підходів (за рахунок додаткових накладних витрат) шляхом динамічного тестування автоматично інструменталізованих додатків, що дозволяє точний моніторинг внутрішнього стану програми у відповідь на зовнішні впливи.

# Багато вразливостей безпеки

Багато вразливостей безпеки дуже важко виявити без ретельного
без ретельної перевірки вихідного коду. Хоча в ідеалі це можна зробити за допомогою експертного або колегіального
це повільне і дороге завдання. Хоча автоматизовані перевірки "шумніші" і часто менш
і часто менш точні, ніж експертні огляди, автоматизовані інструменти SAST дешевші, набагато
швидші і більш послідовні, ніж люди.

## Автоматизовані інструменти SAST

Ряд комерційних і безкоштовних інструментів здатні ефективно виявляти досить важливі помилки та
вразливості у великих базах коду.

## Динамічне тестування

Динамічне тестування не потребує вихідного коду програми, що робить його ідеальним для
випадків, коли вихідний код недоступний. Воно також дозволяє виявити конкретні приклади
вразливостей. Завдяки підходу "чорної скриньки", без інструментарію, воно
він з більшою ймовірністю виявить неглибокі помилки.

## Інструменти динамічного тестування

Інструменти динамічного тестування потребують великого джерела тестових даних, створення яких вручну є надто дорогим. Існує багато інструментів
які генерують відповідні тестові дані автоматично, що призводить до більш ефективного
тестування безпеки та отримання більш якісних результатів.

## Вибір інструментів

Вибирайте відповідні інструменти на основі декількох факторів, включаючи глибину і
глибина і точність перевірки, надійність і точність тестових кейсів безпеки,
можливість інтеграції з іншими інструментами, модель використання та вартості тощо. Під час
вибору інструментів використовуйте інформацію від технічного персоналу, який розуміється на питаннях безпеки, а також
розробників та менеджерів розробки, а також обговоріть результати із зацікавленими сторонами.

# Рівень зрілості 1

## Чи скануєте ви додатки за допомогою автоматизованих інструментів тестування безпеки?

### Критерії якості

- Ви динамічно генеруєте вхідні дані для тестів безпеки за допомогою автоматизованих інструментів
- Ви обираєте інструменти тестування безпеки відповідно до архітектури та технологічного стеку організації, а також балансуєте між глибиною і точністю перевірки та зручністю використання результатів для організації

### Відповіді

- Ні
- Так, деякі з них
- Так, принаймні половина з них
- Так, більшість або всі з них

# Рівень зрілості 2

## Переваги

- Виявлення вразливостей, характерних для організації, які легко знайти

## Діяльність

Підвищуйте ефективність автоматизованих інструментів тестування безпеки, налаштовуючи та налаштувавши їх для ваших конкретних технологічних стеків та додатків.

Автоматизовані інструменти тестування безпеки мають 2 важливі характеристики:

1. Рівень хибних спрацьовувань, тобто кількість помилкових помилкових спрацьовувань, тобто неіснуючих помилок і вразливостей, про які вони помилково повідомляють.
2. Рівень хибнонегативних результатів, тобто фактичні помилки та вразливості, які вони не змогли виявити.

У міру того, як ви стаєте досвідченим у використанні інструментів автоматизованого тестування, ви прагнете мінімізувати кількість хибнопозитивних та хибнонегативних спрацьовувань. Це максимізує час, який команди розробників витрачають на аналіз та вирішення реальних проблем безпеки у своїх додатках, а також зменшує тертя, які зазвичай пов'язані з використанням ненастроєних автоматизованих інструментів аналізу безпеки.

### Кроки дії

1. Почніть з вимкнення підтримки інструментів для технологій і фреймворків, які ви не використовуєте, і, де це можливо, орієнтуйтеся на конкретні версії. Це збільшить швидкість виконання і зменшить кількість помилкових результатів.

2. Покладайтеся на лідерів у сфері безпеки чемпіонів або спільні команди безпеки для пілотування інструментів у координації з обраною групою мотивованих команд розробників. Це дозволить виявити ймовірні хибні позитивні результати, які можна ігнорувати або вилучити з результатів роботи інструментів.

3. Визначте конкретні проблеми безпеки та антипатерни і оберіть найкращий інструмент для їх виявлення.

# Використання статичного аналізу для підвищення безпеки програмного забезпечення

Використовуйте доступні функції інструментів, щоб врахувати специфіку програми та організаційні стилі кодування, а також технічних стандартів. Багато автоматизованих статичного аналізу дозволяють користувачам писати власні правила або налаштовувати стандартні правила аналізу за замовчуванням до конкретних програмних інтерфейсів у проекті, що тестується, для підвищення точності та глибини покриття.

Наприклад, потенційно небезпечні вхідні дані (так звані зіпсовані) можуть бути позначені як безпечні після того, як вони пройдуть через спеціальний метод санітарної обробки.

Зі стратегічної точки зору, краще надійно виявляти обмежену підмножину проблем безпеки за допомогою автоматизованого інструментарію і поступово розширювати покриття, ніж намагатися негайно виявити всі відомі проблеми. Після того, як інструменти будуть достатньо налаштовані, їх можна зробити доступними для більшої кількості команд розробників. Важливо постійно відстежувати їхню ефективність серед команд розробників.

У більш просунутих формах методи машинного навчання можуть бути застосовані для виявлення та автоматичного відсіювання ймовірних помилкових спрацьовувань в масштабі.

Заголовок "Рівень зрілості 2"

Чи налаштовуєте ви автоматизовані інструменти безпеки для своїх додатків та стеків технологій?

### Критерії якості

1. Ви налаштовуєте та обираєте функції інструментів, які відповідають вашому застосунку чи стеку технологій.
2. Ви мінімізуєте помилкові спрацьовування шляхом ігнорування або автоматичного фільтрування нерелевантних попереджень або маловірогідних знахідок.
3. Ви мінімізуєте помилкові негативні результати, використовуючи розширення інструментів або DSL для налаштування інструментів для вашої програми або стандартів організації.

### Відповіді

- Ні
- Так, деякі з них
- Так, принаймні половина з них
- Так, більшість або всі з них

## Рівень зрілості 3

**Переваги**

- Виявлення вразливостей, що виявляються автоматично, на якомога більш ранніх стадіях.

**Діяльність**

- Проекти в організації регулярно проходять автоматизовані тести безпеки та переглядають результати під час розробки.
- Налаштуйте інструменти тестування безпеки на автоматично запускатися як частина процесу збірки та розгортання, щоб зробити його масштабованим з низькими накладними витратами.
- Перевіряйте результати одразу після їх отримання.
- Проведення тестів безпеки ще на етапі формування вимог або проектування може бути корисним.
- Хоча традиційно він використовується для функціональних тестових кейсів, цей тип підходу до розробки на основі тестування передбачає визначення та запуск відповідних тестових кейсів на ранніх етапах циклу розробки.
- Завдяки автоматичному виконанню тестових кейсів безпеки, проекти переходять до фази реалізації з низкою невдалих тестів для неіснуючої функціональності.
- Реалізація вважається завершеною, коли всі тести пройдені.
- Це забезпечує чітку, заздалегідь визначену мету для розробників на початку циклу розробки, знижуючи ризик затримки релізу через міркування з міркувань безпеки або вимушеного прийняття ризику для дотримання термінів проекту.

## Зробіть результати автоматизованих і ручних тестів безпеки видимими

Зробіть результати автоматизованих і ручних тестів безпеки видимими через інформаційних панелей і регулярно надавайте їх керівництву та зацікавленим сторонам перед кожним випуском для ознайомлення.

Якщо є невирішені ризики, які залишаються прийнятними для релізу, зацікавлені сторони та менеджери розробки працюють разом, щоб встановити конкретні часові рамки для їх вирішення.

### Постійно переглядайте та покращуйте якість тестів безпеки

Розгляньте та впровадьте інструменти кореляції тестів безпеки для автоматизації зіставлення та об'єднання результатів тестів від динамічних, статичних та інтерактивних сканерів додатків в одну центральну інформаційну панель, забезпечуючи прямий вхід для управління дефектами.

### Поширюйте знання про створені тести безпеки

Поширюйте знання про створені тести безпеки та їх результатів серед команди розробників, щоб покращити знання та обізнаність про безпеку в організації, сприяючи підвищенню обізнаності всередині організації.

# Рівень зрілості 3

Чи використовуєте ви результати тестування безпеки для покращення життєвого циклу розробки життєвого циклу розробки?

## Критерії якості

- Ви використовуєте результати інших заходів з безпеки для покращення інтегрованого тестування безпеки під час розробки
- Ви переглядаєте результати тестування та включаєте їх у тренінги з безпеки та інструкції з тестування безпеки
- Зацікавлені сторони переглядають результати тестування та обробляють їх відповідно до управління ризиками організації

## Відповіді

1. Ні
2. Так, але ми вдосконалюємо його від випадку до випадку
3. Так, ми вдосконалюємо її на регулярній основі
4. Так, ми вдосконалюємо його щонайменше раз на рік
